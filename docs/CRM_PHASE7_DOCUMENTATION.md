# 📚 مستندات فاز 7: گزارشات و داشبورد

## 📋 فهرست مطالب
- [معرفی](#معرفی)
- [صفحات ایجاد شده](#صفحات-ایجاد-شده)
- [ویژگیها](#ویژگیها)
- [نمودارها](#نمودارها)
- [نحوه استفاده](#نحوه-استفاده)

---

## معرفی

فاز 7 شامل داشبورد مدیریت و سیستم گزارشات است که به ادمین امکان میدهد:
- مشاهده آمار کلی سیستم
- تحلیل درآمد ماهانه
- بررسی وضعیت پروژهها
- تولید گزارشات
- Export به CSV

---

## صفحات ایجاد شده

### 1. Dashboard.tsx
**مسیر:** `/admin/dashboard`

**ویژگیها:**
- 4 کارت آماری (مشتریان، پروژهها، درآمد، تیکتها)
- نمودار خطی درآمد ماهانه
- نمودار دایرهای وضعیت پروژهها
- آمار Real-time

**کامپوننتها:**
- LineChart - نمودار درآمد
- PieChart - وضعیت پروژهها
- Cards - آمار کلی

### 2. Reports.tsx
**مسیر:** `/admin/reports`

**ویژگیها:**
- انتخاب نوع گزارش (درآمد، پروژهها، مشتریان)
- انتخاب دوره زمانی (ماهانه، سالانه)
- نمودار میلهای
- جدول دادهها
- Export به CSV

**گزارشات:**
- گزارش درآمد
- گزارش پروژهها
- گزارش مشتریان جدید

---

## ویژگیها

### داشبورد
- ✅ آمار کلی سیستم
- ✅ نمودار درآمد ماهانه
- ✅ نمودار وضعیت پروژهها
- ✅ بهروزرسانی Real-time

### گزارشات
- ✅ گزارشات قابل تنظیم
- ✅ نمودار میلهای
- ✅ جدول دادهها
- ✅ Export به CSV
- ✅ فیلتر دوره زمانی

---

## نمودارها

### استفاده شده:
- **Recharts** - کتابخانه نمودار
- **LineChart** - نمودار خطی
- **BarChart** - نمودار میلهای
- **PieChart** - نمودار دایرهای

### نصب:
```bash
npm install recharts
```

---

## نحوه استفاده

### 1. افزودن Routes

```typescript
import AdminDashboard from "@/pages/admin/Dashboard";
import Reports from "@/pages/admin/Reports";

<Route path="/admin/dashboard" element={<AdminDashboard />} />
<Route path="/admin/reports" element={<Reports />} />
```

### 2. استفاده از نمودارها

```typescript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer } from "recharts";

<ResponsiveContainer width="100%" height={300}>
  <LineChart data={data}>
    <CartesianGrid strokeDasharray="3 3" />
    <XAxis dataKey="month" />
    <YAxis />
    <Tooltip />
    <Legend />
    <Line type="monotone" dataKey="amount" stroke="#8884d8" />
  </LineChart>
</ResponsiveContainer>
```

### 3. Export به CSV

```typescript
const exportToCSV = () => {
  const headers = Object.keys(data[0]).join(",");
  const rows = data.map(row => Object.values(row).join(",")).join("\n");
  const csv = `${headers}\n${rows}`;
  
  const blob = new Blob([csv], { type: "text/csv" });
  const url = window.URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = `report-${Date.now()}.csv`;
  a.click();
};
```

---

## مسیرها

| مسیر | صفحه | توضیحات |
|------|------|---------|
| `/admin/dashboard` | Dashboard | داشبورد اصلی |
| `/admin/reports` | Reports | گزارشات |

---

## فایلهای ایجاد شده

```
src/pages/admin/
├── Dashboard.tsx
└── Reports.tsx

docs/
└── CRM_PHASE7_DOCUMENTATION.md
```

---

## نکات مهم

1. **Recharts:** نیاز به نصب کتابخانه recharts
2. **Performance:** نمودارها بهینه شده برای دادههای زیاد
3. **Responsive:** تمام نمودارها Responsive هستند
4. **Export:** امکان Export به CSV

---

## Dependencies

```json
{
  "recharts": "^2.10.0"
}
```

---

## مراحل بعدی

- [ ] افزودن گزارشات پیشرفتهتر
- [ ] Export به PDF
- [ ] فیلترهای بیشتر
- [ ] نمودارهای بیشتر

---

**تاریخ ایجاد:** 2025-01-15  
**نسخه:** 1.0.0
