# 📚 مستندات فاز 2: مدیریت مشتریان (CRM)

## ✅ وضعیت: تکمیل شده

تاریخ تکمیل: 2025-01-15

---

## 📋 خلاصه فاز 2

در این فاز، صفحات و کامپوننتهای مدیریت مشتریان ایجاد شد:

- ✅ صفحه لیست مشتریان با جستجو و فیلتر
- ✅ فرم افزودن/ویرایش مشتری
- ✅ صفحه پروفایل مشتری
- ✅ نمایش آمار و تاریخچه
- ✅ یکپارچهسازی با Routes

---

## 📝 فایلهای ایجاد شده

### 1. صفحه لیست مشتریان
```
src/pages/admin/Customers.tsx
```

**ویژگیها:**
- نمایش لیست تمام مشتریان
- جستجو بر اساس نام، ایمیل، شماره تلفن
- نمایش آمار (کل، فعال، غیرفعال)
- کارت برای هر مشتری با آواتار رنگی
- لینک به پروفایل مشتری
- دکمه افزودن مشتری جدید

### 2. فرم مشتری
```
src/pages/admin/CustomerForm.tsx
```

**ویژگیها:**
- فرم واحد برای افزودن و ویرایش
- Validation با Zod
- فیلدها:
  - نام و نام خانوادگی *
  - ایمیل *
  - شماره تلفن
  - نام شرکت
  - کد ملی/شناسه ملی
  - آدرس
  - شهر
  - کد پستی
  - وضعیت *
  - یادداشتها
- پیامهای خطا
- Loading states

### 3. پروفایل مشتری
```
src/pages/admin/CustomerProfile.tsx
```

**ویژگیها:**
- نمایش اطلاعات کامل مشتری
- آواتار رنگی با حروف اول نام
- Badge وضعیت
- آمار:
  - تعداد پروژهها
  - تعداد فاکتورها
  - تعداد تیکتها
  - مبلغ پرداخت شده
  - مبلغ پرداخت نشده
- تبهای:
  - پروژهها
  - فاکتورها
  - تیکتها
- دکمههای:
  - ویرایش
  - حذف (با تأیید)

---

## 🛣️ Routes اضافه شده

```typescript
/admin/customers              → لیست مشتریان
/admin/customers/new          → افزودن مشتری جدید
/admin/customers/:id          → پروفایل مشتری
/admin/customers/:id/edit     → ویرایش مشتری
```

---

## 🎨 UI/UX Features

### آواتار رنگی
- تولید رنگ بر اساس نام
- نمایش حروف اول نام
- 8 رنگ مختلف

### Badge وضعیت
- فعال → سبز
- غیرفعال → خاکستری
- مسدود → قرمز

### جستجو
- Real-time search
- جستجو در نام، ایمیل، تلفن
- بدون نیاز به دکمه

### Responsive
- موبایل فرست
- Grid responsive
- کارتهای انعطافپذیر

---

## 🔧 استفاده از Helper Functions

```typescript
// گرفتن لیست مشتریان
const { data: customers } = useQuery({
  queryKey: ["customers"],
  queryFn: getCustomers,
});

// گرفتن یک مشتری
const { data: customer } = useQuery({
  queryKey: ["customer", id],
  queryFn: () => getCustomerById(id),
});

// ایجاد مشتری
const createMutation = useMutation({
  mutationFn: createCustomer,
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["customers"] });
  },
});

// بهروزرسانی مشتری
const updateMutation = useMutation({
  mutationFn: (data) => updateCustomer(id, data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["customers"] });
  },
});

// حذف مشتری
const deleteMutation = useMutation({
  mutationFn: () => deleteCustomer(id),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ["customers"] });
  },
});
```

---

## 📊 آمار فاز 2

| مورد | تعداد |
|------|-------|
| صفحات جدید | 3 |
| Routes جدید | 4 |
| خطوط کد | ~800 |
| کامپوننتها | 3 |

---

## ✨ ویژگیهای پیادهسازی شده

- ✅ **CRUD کامل** برای مشتریان
- ✅ **Validation** با Zod
- ✅ **Real-time Search**
- ✅ **Loading States**
- ✅ **Error Handling**
- ✅ **Toast Notifications**
- ✅ **Responsive Design**
- ✅ **Confirmation Dialogs**

---

## 🎯 تست

### چک لیست تست:

- [ ] افزودن مشتری جدید
- [ ] ویرایش مشتری موجود
- [ ] حذف مشتری
- [ ] جستجو در لیست
- [ ] مشاهده پروفایل
- [ ] نمایش آمار
- [ ] تست در موبایل
- [ ] تست validation

---

## 🔄 مراحل بعدی (فاز 3)

فاز بعدی شامل موارد زیر خواهد بود:

1. ✅ Kanban Board برای پروژهها
2. ✅ فرم ایجاد و ویرایش پروژه
3. ✅ آپلود فایل
4. ✅ تایملاین پروژه

---

**وضعیت:** ✅ تکمیل شده و آماده استفاده
